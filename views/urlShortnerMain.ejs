<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            color: white;
            background: linear-gradient(45deg, #141e30, #243b55);
            margin: 0;
            padding: 2rem;
        }
        h1 {
            text-align: center;
            font-size: 2.5rem;
            color: #f39c12;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
            margin-bottom: 2rem;
        }
        ul {
            list-style: none;
            padding: 0;
            margin: 0 auto;
            max-width: 600px;
        }
        li {
            background-color: #2c3e50;
            margin: 1rem 0;
            padding: 1rem;
            border-radius: 8px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }
        li label {
            color: #2ecc71;
            font-size: 1rem;
            font-weight: bold;
            margin-right: 1rem;
        }
        li a {
            text-decoration: none;
            color: #3498db;
            font-weight: bold;
            transition: color 0.3s ease;
        }
        li a:hover {
            color: #1abc9c;
        }
        button {
            padding: 0.8rem 1.5rem;
            font-size: 1rem;
            font-weight: bold;
            color: white;
            background: #e74c3c;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
            display: block;
            margin: 2rem auto 0;
        }
        button:hover {
            background: #c0392b;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            transform: scale(1.05);
        }
        button:focus {
            outline: none;
        }
    </style>
</head>
<body>
    <h1>Welcome to the URL Shortener Home Page</h1>
    <ul>
        <% urls.forEach(url => { %>
            <li>
                <label>Original URL:</label>
                <span><%= url.url %></span>
                <a href="/url/<%= url.shortner %>" target="_blank"> <%= url.shortner %></a>
               
            </li>
        <% }) %>
    </ul>
    <form method="get" action="/url/add">
        <button>Add New URL</button>
    </form>
   
</body>
</html>
